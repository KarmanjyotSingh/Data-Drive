services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    image: backend

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.chonky-frontend
    image: frontend
    ports:
      - "3000:3000"